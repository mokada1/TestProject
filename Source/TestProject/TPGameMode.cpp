// Copyright Epic Games, Inc. All Rights Reserved.

#include "TPGameMode.h"

//void ATPGameMode::PrintErrors()
//{
//	auto errors = TPError::GetInstance().GetErrors();
//	for (auto it = errors.begin(); it != errors.end(); ++it)
//	{
//		auto error = *it;
//		if (wcscmp(error, L"") == 0)
//		{
//			continue;
//		}
//		UE_LOG(LogTemp, Log, TEXT("Error: %s"), error);
//	}
//	errors.clear();
//}
